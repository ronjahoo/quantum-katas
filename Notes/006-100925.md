# Continued Complex Arithmetic

Exercise 9
- once again, the formulas took a bit of time, but the calculation itself was easy

```
@exercise
def polar_convert(x : Complex) -> Polar:
    r = math.sqrt(x[0] ** 2 + x[1] ** 2)
    theta = math.atan2(x[1], x[0])

    return (r, theta)
```