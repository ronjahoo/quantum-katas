# Continued Linear Algebra Section

Exercise 13
- At the end of the day, an easy task, but I made a few mistakes and forgot I had the multiplication function made already, so took a bit more time than necessary

```
@exercise
def find_eigenvalue(a : Matrix, v : Matrix) -> float:
    n = len(v)
    multiplied = matrix_mult(a, v)

    for i in range(n):
        if (v[i][0] != 0):
            return multiplied[i][0] / v[i][0]
```