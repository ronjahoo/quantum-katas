# Continued with single qubit gates

Exercise 2
- Needed to think a bit about the Pauli matrices' functionalities, but it was an easy task after that

```
%kata T2_GlobalPhaseI

operation GlobalPhaseI (q : Qubit) : Unit is Adj+Ctl {
    Z(q);
    Y(q);
    X(q);
}
```