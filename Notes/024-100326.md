# Started with single qubit gates

Exercise 1
- I started to do it from scrach like in the examples, then I noticed that it only requires the Y gate, literally

```
%kata T1_ApplyY

operation ApplyY (q : Qubit) : Unit is Adj+Ctl {
    Y(q);
}
```